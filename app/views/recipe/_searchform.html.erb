<%= form_tag(recipes_path, method: "get", class: "input-group") do %>
  <%= text_field_tag(:q, "What's for dinner?", class: "input-group-field") %>
  <%= submit_tag("Let's do this!", class: "button input-group-button") %>
<% end %>
