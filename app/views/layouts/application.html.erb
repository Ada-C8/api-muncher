<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />

    <title><%= content_for?(:title) ? yield(:title) : "Untitled" %></title>

    <%= stylesheet_link_tag    "application" %>
    <%= javascript_include_tag "application", 'data-turbolinks-track' => true %>
    <%= csrf_meta_tags %>
  </head>

  <body>
    <% unless params[:action] == "home"%>
      <header class="row expanded">
        <h1 class="column small-12 medium-6">Find Me the Recipe</h1>
        <div class="column small-12 medium-6"><%= render partial: "searchform" %>
</div>
      </header>
    <% end %>

    <main class="row">
      <%= yield %>
    </main>

    <section class="row debug">
      <%= debug(params) if Rails.env.development? %>
    </section>

    <footer class="row">
      <div class="column small-12 text-center">
        Copyright 2017. Powered by <%= link_to image_tag("edamam_logo.png"), "https://www.edamam.com/" , alt: "Edamam Logo"%>
      </div>
    </footer>
  </body>
</html>
