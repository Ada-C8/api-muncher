<!DOCTYPE html>
<html lang="en">
  <head>
    <title>TITLE</title>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />

    <title><%= content_for?(:title) ? yield(:title) : "Untitled" %></title>

    <%= stylesheet_link_tag    "application" %>
    <%= javascript_include_tag "application", 'data-turbolinks-track' => true %>
    <%= csrf_meta_tags %>
  </head>
  <!-- will not show on root page -->
  <% unless current_page?(root_path) %>
    <nav>
      <div class= "row text-center">
        <h1 class="nav-main"><%= link_to "MUNCHER", root_path %></h1>
        <div class="small-4 medium-6 cell small-centered columns">
          <%= form_tag recipes_path, method: "get" do %>
          <%= text_field_tag(:search) %>
          <%= submit_tag("Search", class: "button nav-button submit")%>
        </div>
        <% end %>
      </div>
    </nav>
  <% end %>

  <body>

    <%= yield %>

  </body>

  <footer class="<%= current_page?(root_path)? '' : 'pad' %>">
    <div class= "row text-center">
      <p id="sticky">
        <span>Powered by Edamam</span>
      </p>
    </div>
  </footer>
</html>
