<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />

    <title><%= content_for?(:title) ? yield(:title) : "MunchSquad" %></title>
    <link href="https://fonts.googleapis.com/css?family=Lobster|Permanent+Marker|Roboto" rel="stylesheet">
    <%= stylesheet_link_tag    "application" %>
    <%= javascript_include_tag "vendor/modernizr" %>
    <%= csrf_meta_tags %>
  </head>

  <body>
    <header>
        <h1><%= link_to "MunchSquad", root_path %></h1>
    </header>

    <main>
        <section class="row status">
        <% if flash[:result_text] or flash[:messages] %>
            <p><%= flash[:result_text] %></p>
            <% if flash[:messages] %>
              <ul>
                <% flash[:messages].each do |name, problems| %>
                  <% problems.each do |problem| %>
                    <li>
                      <strong><%= name.capitalize %></strong> <%= problem %>
                    </li>
                  <% end %>
                <% end %>
              </ul>
            <% end %>

        <% end %>
        </section>
    <%= yield %>
    </main>

    <%= javascript_include_tag "application" %>
  </body>
</html>
