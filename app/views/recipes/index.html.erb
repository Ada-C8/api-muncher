<h1>MUNCHER</h1>

<h2>Find your flavor</h2>

<%= form_tag(recipes_search_path(:query), method: :get, id: "main-search") do %>
  <%= label_tag :query %>
  <%= text_field_tag :query, params[:query]%>
  <%= submit_tag 'Search', name: nil %>
<% end %>

<%# if @recipes.blank? %>
  <!-- <h4>There are no recipes containing the term <%# params[:query] %>.</h4> -->
<%# end %>
