<section class="homepage">
  <h1>Search</h1>
  <ul>
    <% @channels.each do |channel| %>
      <li>
        <%= link_to channel.name, chat_new_path(channel.id) %>
      </li>
    <% end %>
  </ul>
</section>
