<h1>Here are you results for: <%= @query %> </h2>
<ul>
  <% @recipes.each do |recipe| %>
    <li>
      <img src="<%= recipe.image %>", alt="<%= recipe.name %>"/>
      <%= link_to recipe.name, recipe_path(recipe.name, recipe_id: recipe.id)%>
    </li>
  <% end %>
</ul>
